import 'package:flutter/material.dart';

class LanguageOrderScreen extends StatelessWidget {
  const LanguageOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Language Order'),
      ),
      body: Container(),
    );
  }
}
